anaconda_config_version: 1
upstream_sources:
  - webpage:
      url: https://poppler.freedesktop.org/releases.html
      regex: poppler-([\d.]+)\.tar\.xz
    packages:
      - poppler
      - poppler-qt
